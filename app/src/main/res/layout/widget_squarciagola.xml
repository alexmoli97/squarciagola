<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout deliberatamente povero: solo viste che le RemoteViews accettano da sempre e nessun
  riferimento che debba risolversi nel tema del launcher. La barra di avanzamento e' stata
  tolta: era il pezzo piu' a rischio e diceva la cosa meno importante.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/radice"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@drawable/widget_sfondo"
    android:padding="14dp"
    android:gravity="center_vertical">

    <ImageView
        android:id="@+id/copertina"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_launcher"
        android:contentDescription="@string/app_name" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="14dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/titolo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="@string/app_name"
            android:textColor="#FFE6E6EC"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/artista"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:text="@string/widget_descrizione"
            android:textColor="#FFB0B0BC"
            android:textSize="13sp"
            android:layout_marginTop="2dp" />

        <TextView
            android:id="@+id/azione"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/widget_canta"
            android:textColor="#FF7BE3A3"
            android:textSize="13sp"
            android:textStyle="bold" />
    </LinearLayout>
</LinearLayout>
