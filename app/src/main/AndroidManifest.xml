<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Aggiornamento in proprio, senza passare da uno store -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Necessarie per disegnare sulla Surface di Android Auto -->
    <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" />
    <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" />

    <!-- Android TV e Fire TV: nessuna di queste e' obbligatoria, altrimenti l'app risulterebbe
         incompatibile con i televisori, che non hanno touch e spesso nemmeno GPS o fotocamera. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <!-- Da Android 11 un'app non vede gli altri pacchetti se non li dichiara. Senza questo
         l'SDK di Spotify non riesce a raggiungere l'app Spotify e la connessione resta
         appesa senza errore, che e' il peggiore dei fallimenti possibili. -->
    <queries>
        <package android:name="com.spotify.music" />
    </queries>

    <application
        android:name=".SquarciagolaApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Squarciagola">

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Stessa Activity sul launcher dei televisori: l'interfaccia e' gia' scura
                     e centrata, e il karaoke a schermo pieno e' esattamente cio' che serve li'. -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <!-- Il redirect lo riceve l'attivita' della libreria di autorizzazione di Spotify,
                 dichiarata nel suo manifest. Dichiararlo anche qui farebbe comparire un
                 selettore fra due applicazioni per la stessa destinazione. -->
        </activity>

        <service
            android:name=".PlaybackService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".car.SquarciagolaCarAppService"
            android:exported="true"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
        </service>

    </application>
</manifest>
